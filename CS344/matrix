#!/bin/bash

#given m1, m2

#Input:  MxN matrix
#Output: 1xN row vector (each element is mean of associated column)
mean(){
	m = $1
	#for each column, convert to row
	col = 0
	row = 0
	
	for i in $m1
	do
		cut -f i $1
		#i++ to skip spaces/tabs
		i = $((i++))
	done

}

#Input:  Row of matrix 
#Output: Mean of row
meanRow(){
	echo 'running meanRow()'
	m = $1
	read $m
	echo 'read $m'
	sum = 0
	count = 0
	echo 'running loop'
	for i in $m
	do
		sum = 'expr $sum + $i'
		count = 'expr $count = 1'
	done
	echo 'completed loop'
	mean = $(( ( $sum + ( $count/2 ) * ( ( $sum > 0 ) * 2-1 ) ) /$count ))
	#returns the mean of the row to stdout
	echo "mean"
}

